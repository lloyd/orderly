1. backticks to include jsonschema properties/values directly in
   an orderly schema (gno's idea)
  a. maxDecimal
  b. title/description (??)
  c. format
2. union type handling (support only schema arrays in type?
   Somehow convert all other cases automagically? 
3. sh1ttons of broken jsonschemas!
4. simple CGI reformatter program?
5. orderly-json.org udpate

other/later:
1. extends
2. disallow
